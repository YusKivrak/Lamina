# Lamina
Simple React-based page for uploading an image and placing text behind it on a canvas.

Open `index.html` in a modern browser to use the app. The UI lets you upload an image, type the text, set the font size, color, and text position, then download the result.
